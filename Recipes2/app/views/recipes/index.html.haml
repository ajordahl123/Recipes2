#main.container
%h1.header Recipe  Hello Foodies! (Homepage)
%h2= link_to "Login", new_user_registration_path 
- if @user.nil?
 %p= "No user singed in yet"
- else
 %p= "User is "+ @user.email

%table.table
 %thead
  %tr
   %th Recipe Name
   %th Meal type 
 %tbody
  -@recipes.each do |r|
   %tr 
    %td= "#{r.recipe_name}"
    %td= "#{r.meal_type}"

= link_to "Create a new recipe", new_recipe_path
