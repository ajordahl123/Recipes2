#main.container
  %h1.header Chef Details
  %h2.header= @user.email

  %h1.image= image_tag @user.image.attached? ? @user.image.variant(resize: "300x400^") : 'default_user.jpg'
  %h2.image= "Upload Chef Image"

  =link_to "Add a new recipe", new_recipe_path
  .row
  = "#{@user.username}"
